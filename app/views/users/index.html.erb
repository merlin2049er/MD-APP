<% @user = current_user %>
<% if user_signed_in? && @user.admin %>

<div class="table-responsive">

<!-- <h1 class="panel-heading">Users Listing<br/>
  <small>Show me the money!</small>
</h1>
-->

    <div class="panel-heading">Total User Listing: <% if @total == 0  %>
       Nothing here...
       <% else %>
      <%= @total %>
      <% end %>

      </div>
    <div class="panel-body">

      
        <table class="table">
        <thead>
        <tr>
          <th>Id </th>
          <th>Username </th>
          <th>First name</th>
          <th>Last name</th>
          <th>Email</th>


        </tr>
        </thead>

        <tbody>
        <% @users.each do |user| %>
          <tr>

            <td><%= user.id  %></td>
            <td><%=  user.username %></td>
            <td><%=  user.firstname  %></td>
            <td><%=  user.lastname  %></td>
            <td><%=  user.email  %></td>


          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
  </div>

  <div class ='pagenav'>
    <%== pagy_bootstrap_nav(@pagy) %> <br/>
  </div>
</div>


<% else %>
  <div class="alert alert-warning" role="alert">You must be the administrator to see the users.</div>
<% end %>
