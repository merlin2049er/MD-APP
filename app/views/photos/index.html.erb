<% if user_signed_in? %> 

<p id="notice"><%= notice %></p>

<!--
<h1>Products</h1>
-->
<!--
  <%#= render 'layouts/ransack' %>
-->

<!--
  <%#= @products.product.size %> records<br/>
  -->
    <%=   @photos.count %> Photos<br/><br/>

<table>
  <thead>
    <tr>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
  <th>Id </th><th> Product Id </th><th>Uri</th>
    <% @photos.each do |photo| %>
      <tr>

         <td><%= link_to photo.id , photo  %></td>
        <td><%=  photo.product_id  %></td>
        <td><%=  photo.uri  %></td>
        <!-- 
        <td><%#= photo.id %></td>
      
        
        <td><%= link_to 'Show', photo %></td>
        <td><%#= link_to 'Edit', edit_photo_path(photo) %></td> -->
        <td><%= link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure?' } %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= will_paginate @photo, renderer: BootstrapPagination::Rails %><br/>

<!-- 
<%= link_to 'New Photo', new_photo_path %>
-->

<% else %>


<!-- <h1>You must be signed in to see the products.</h1> -->

<div class="alert alert-warning" role="alert"> You must be signed in to see the photos.</div>
  
<% end %> 

